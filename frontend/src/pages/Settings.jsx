import PageHeader from "../components/common/PageHeader/PageHeader";

export default function Settings() {
  return (
    <div className="p-10">
      <PageHeader
        title="Settings"
        subtitle="Manage application preferences, AI model configurations, notifications, and system settings."
      />
    </div>
  );
}